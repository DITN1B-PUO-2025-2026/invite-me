name: Invite user on issue label
on:
  issues:
    types: [labeled]

jobs:
  invite:
    runs-on: ubuntu-latest
    permissions: # ADD THIS BLOCK for issue commenting
      issues: write
      contents: read
    steps:
      - name: Invite on label
        uses: vj-abigo/invite-on-label@v1.4
        with:
          organization: DITN1B-PUO-2025-2026
          label: ${{ secrets.INVITE_LABEL_KEY }}
          # FIX: Use the powerful PAT for org-level actions and repo-level commenting.
          repo-token: ${{ secrets.INVITE_TOKEN }} 
          comment: 'Welcome to the Org. You have been invited successfully! 🎉'
          existingMemberMessage: 'You are already a member of the organization :D '
          